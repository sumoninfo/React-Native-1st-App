import React        from 'react';
import {Text, View} from "react-native";

const NotFound = () => {
    return (
        <View>
            <Text>NotFound Page</Text>
        </View>
    );
};

export default NotFound;
