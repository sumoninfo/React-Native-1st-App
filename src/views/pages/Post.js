import React        from 'react';
import {Text, View} from "react-native";

const Post = () => {
    return (
        <View>
            <Text>Single Post Page</Text>
        </View>
    );
};

export default Post;
